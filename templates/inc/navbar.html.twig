<nav class="navbar navbar-expand-sm navbar-dark bg-primary mb-3">
    <div class="container">
        <a href="/" class="navbar-brand">Symfony</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggle-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mobile-nav">
            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                <li class="nav-item">
                    <a href="{{ path('article_list') }}" class="nav-link text-secondary display-5 bg-light">{{ app.user.username }}</a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a href="{{ path('article_list') }}" class="nav-link">Articles</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('category_index') }}" class="nav-link">Categories</a>
                </li>
                {% if app.user %}
                    <li class="nav-item">
                        <a href="{{ path('app_logout') }}" class="nav-link">Logout</a>
                    </li>
                {% elseif  app.user == null%}
                    <li class="nav-item">
                        <a href="{{ path('app_login') }}" class="nav-link">Login</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('app_register') }}" class="nav-link">SignUp</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>